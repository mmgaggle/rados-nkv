# Build outputs — source is committed; binaries/objects are not.
# (WASM test modules under kv/wasm/*.wasm ARE committed fixtures, so not ignored.
#  The Rust crate (kv/rados-nkv/.gitignore), vfu_host, and cls have their own.)
*.o
*.d
kv/kv_host
kv/kv_ro_host
kv_rados/kv_rados_host
kv_shim/kv_shim_test
